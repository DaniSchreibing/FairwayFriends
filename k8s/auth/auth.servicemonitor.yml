apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: auth
  namespace: default
spec:
  selector:
    matchLabels:
      app: auth
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: metrics
      path: /api/auth/metrics
      interval: 15s
